//LoadText.h
#ifndef _LOAD_TEXT_H
#define _LOAD_TEXT_H

#include <iostream>
#include <string>
#include <fstream>

std::string LoadText(std::string filename);
std::string LoadText(const char *filename);

#endif //_LOAD_TEXT_H
